#include "DayStrategy.h"


DayStrategy::DayStrategy()
{
}

/** Strategy that updates the trigger factor by the number of days */
int DayStrategy::update(Factory* factory, int triggerFactor)
{
    //int days = factory->getDayCount(), newTriggerFactor;

    //newTriggerFactor = triggerFactor+ (days - startDayCount);

    //return newTriggerFactor;
}
